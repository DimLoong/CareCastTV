<?xml version="1.0" encoding="utf-8"?>
<!--
  CareCastTV 安卓电视壳应用
  全屏 WebView 加载部署好的 CareCastTV 站点，支持开机自启与遥控器操作。
  维护者：DimLoong
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 电视设备通常没有触摸屏，声明为非必需以便安装 -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <!-- 支持 Android TV Launcher，但不强制（普通盒子也能装） -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.CareCastTV"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic=true：允许 http:// 局域网地址（如 NAS 部署）。
             若只用 https 的 Vercel 域名，可改为 false -->

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- 同时注册手机桌面与 Android TV 桌面入口 -->
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 开机自启：部分 Android 10+ 设备会限制后台拉起界面，
             需在系统设置中为本应用授予自启权限 -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
